name: habit_boost
description: "HabitBoost — приложение для формирования полезных привычек"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  # cloud_firestore: ^5.6.0   # TODO(sergey): uncomment when backend is ready
  connectivity_plus: ^6.1.2
  dartz: ^0.10.1
  drift: ^2.23.1
  equatable: ^2.0.7
  # firebase_analytics: ^11.5.0  # TODO(sergey): uncomment when backend is ready
  # firebase_auth: ^5.5.0        # TODO(sergey): uncomment when backend is ready
  # firebase_core: ^3.12.0       # TODO(sergey): uncomment when backend is ready
  # firebase_crashlytics: ^4.3.0 # TODO(sergey): uncomment when backend is ready
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  flutter_secure_storage: ^9.2.4
  flutter_svg: ^2.0.17
  freezed_annotation: ^3.0.0
  get_it: ^8.0.3
  go_router: ^15.1.0
  injectable: ^2.5.0
  intl: ^0.20.2
  json_annotation: ^4.9.0
  logger: ^2.5.0
  path: ^1.9.1
  path_provider: ^2.1.5
  shared_preferences: ^2.3.4
  shimmer: ^3.0.0
  sqlite3_flutter_libs: ^0.5.28

dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.4.14
  drift_dev: ^2.23.1
  flutter_test:
    sdk: flutter
  freezed: ^3.0.0
  injectable_generator: ^2.7.0
  json_serializable: ^6.9.4
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
